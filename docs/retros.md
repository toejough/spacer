# Spacer — Retrospectives

### Increment #2: Rebuild vertical slice bootstrap — pass
**What worked:** Light process (plan → execute → review) delivered a working app in one pass; vertical slice means every file contains real code.
**What to improve:** First attempt jumped straight to execution without writing the plan, despite having the retro, updated skill, and prompt all saying "plan first." Information doesn't change behavior; structure does.
**Action items:**
- Added hard gate to 5m-increment skill: 3+ files → written plan in `docs/plans/` + user approval before any code. Plan is a committed deliverable, not a process step.
