## 1. Update Go source

- [x] 1.1 `srv/server.go`: remove the `"srv.exe.dev/db"` import, the `db.Open`/`db.RunMigrations` block in `New()`, and the `DBPath` field from `Server`
- [x] 1.2 `srv/server.go`: change `New()`'s signature to `New(hostname string) (*Server, error)`
- [x] 1.3 `cmd/srv/main.go`: update the call site to `srv.New(hostname)` (drop the `"db.sqlite3"` argument)
- [x] 1.4 `go build ./...` succeeds (proves nothing else references the `db` package before it's deleted)

## 2. Delete the db/ subsystem

- [x] 2.1 Delete `db/db.go`
- [x] 2.2 Delete `db/sqlc.yaml`
- [x] 2.3 Delete `db/dbgen/` (`db.go`, `items.sql.go`, `models.go`)
- [x] 2.4 Delete `db/migrations/` (all 4 files: `001-base.sql`, `002-todos-notes.sql`, `003-add-timestamps.sql`, `004-drop-visitors.sql`)
- [x] 2.5 Delete `db/queries/items.sql`
- [x] 2.6 Confirm `db/` is now empty and remove the directory

## 3. Clean up the dependency tree

- [x] 3.1 Remove the `tool github.com/sqlc-dev/sqlc/cmd/sqlc` line from `go.mod`
- [x] 3.2 Run `go mod tidy` and review the diff — confirm `modernc.org/sqlite` and sqlc's supporting indirect dependencies are dropped, and nothing the app actually still needs is removed

## 4. Update docs

- [x] 4.1 `README.md`: remove the "## Database" section
- [x] 4.2 `README.md`: remove the `db` bullet from "Code layout"
- [x] 4.3 `openspec/config.yaml`: remove the SQLite/sqlc lines from the `context` block ("SQLite with sqlc (db/)", "the server opens a SQLite database for the template", "Database migrations in db/migrations/, queries in db/queries/, models generated by sqlc")
- [x] 4.4 `.gitignore`: remove the now-moot `db.sqlite3`/`*.db` entries

## 5. Verify

- [x] 5.1 `grep -rn "srv.exe.dev/db\|modernc.org/sqlite\|sqlc" --include="*.go" --include="*.md" --include="*.yaml" .` (excluding `node_modules/`, `.git/`, archived openspec changes, and this change's own planning docs) returns nothing
- [x] 5.2 `make build && make test` pass
- [x] 5.3 Restart the live app (`systemctl restart app.service` or equivalent) and confirm it starts cleanly with no database-related errors in the logs
- [x] 5.4 Load the app in a browser: it renders, a todo can be added and persists (via `localStorage`, unaffected by this change), no console errors
- [x] 5.5 Confirm no `db.sqlite3` file is created on a fresh run (nothing left creates one)
